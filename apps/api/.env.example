# FreeClip API — Environment Variables
# Copy to .env for local development

# Port the API listens on
PORT=4000

# Node environment
NODE_ENV=production

# Log level: "debug" | "info" | "warn" | "error"
# Defaults to "debug" in development, "info" in production
LOG_LEVEL=info
